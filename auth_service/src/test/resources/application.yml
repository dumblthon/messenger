spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/service_auth
    username: test
    password: test
  liquibase.enabled: false
jwt:
  jks.pass: ${JKS_PASS}
  ttl: 604800 # 60 × 60 × 24 × 7
totp:
  secret.length: 128
  code.length: 6
  time:
    period: 30
    discrepancy: 2
sotp:
  expireMins: 5
